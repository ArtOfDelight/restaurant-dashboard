# === AI CONFIGURATION ===
# Option 1: Use Gemini (Google) - FREE tier available but has rate limits
GEMINI_API_KEY=your_gemini_key_here
GEMINI_API_KEY1=backup_key_1  # Optional: Add more keys for rotation
GEMINI_API_KEY2=backup_key_2  # Optional
GEMINI_API_KEY3=backup_key_3  # Optional
GEMINI_API_KEY4=backup_key_4  # Optional

# Option 2: Use Groq (Recommended for FREE unlimited usage)
GROQ_API_KEY=your_groq_key_here

# RECOMMENDED SETUP:
# Set BOTH keys - system will use Gemini first, then auto-fallback to Groq if rate limited
# This gives you the best of both worlds!

# === OTHER SETTINGS ===
SPREADSHEET_ID=your_spreadsheet_id
CHECKLIST_SPREADSHEET_ID=your_checklist_spreadsheet_id
# ... other env vars ...
